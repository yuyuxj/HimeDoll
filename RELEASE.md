# HimeDoll v7.0.0 发布说明

1. 备份数据库、主题与插件。
2. 覆盖 `wp-content/themes/himedoll` 与 `wp-content/plugins/himedoll-core`。
3. 后台停用并重新启用 HimeDoll Core。
4. 进入 **HimeDoll → AI 运营中心** 配置兼容 OpenAI Chat Completions 的 Base URL、API Key 与模型。
5. 先选 1 个测试商品执行“完整商品包”，确认日文与产品事实后再批量运行。
6. 清理页面缓存/CDN缓存并重新保存固定链接。

注意：AI生成内容必须人工审核；不要让模型补写未确认的材质、尺寸、交期、认证或医疗效果。
